<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Jiff</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body <%= "data-server=#{@server.id}" if @server %>>
    <%= render 'header' %>
    <main>
      <div class="row panel">
		    <%= yield %>
      </div>
    </main>
    <%= javascript_include_tag "application" %>
  </body>
</html>