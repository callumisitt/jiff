<%= render 'site_sidebar' %>

<div class="small-12 medium-9 columns">
  <div class="panel">
    <%= render 'site_links', site: @site %>

    <%= simple_form_for @site, url: view_log_server_site_path(@server, @site) do |f| %>
      <%= f.input :input, label: false, as: :text, disabled: true, input_html: { value: @site.view_log, rows: 40 } %>
    <% end %>
  </div>
</div>